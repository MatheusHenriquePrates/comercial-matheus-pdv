# ===========================================
# COMERCIAL MATHEUS PDV - BACKEND ENVIRONMENT
# ===========================================
# Copie este arquivo para .env e configure os valores

# ===== BANCO DE DADOS =====
# URL de conexão com o PostgreSQL
DATABASE_URL="postgresql://usuario:senha@localhost:5432/comercial_matheus_pdv"

# ===== SEGURANÇA =====
# IMPORTANTE: Gere chaves seguras com: openssl rand -hex 32
# NUNCA use as chaves de exemplo em produção!

# Chave secreta para assinatura de tokens JWT (mínimo 32 caracteres)
JWT_SECRET="GERE_UMA_CHAVE_SEGURA_COM_OPENSSL_RAND_HEX_32"

# Chave para criptografia de dados sensíveis (mínimo 32 caracteres)
# Usada para criptografar PIN do certificado, CSC Token, etc.
ENCRYPTION_KEY="GERE_UMA_CHAVE_SEGURA_COM_OPENSSL_RAND_HEX_32"

# ===== SERVIDOR =====
PORT=3333
NODE_ENV=development

# ===== GERAR CHAVES SEGURAS =====
# Execute no terminal:
#   openssl rand -hex 32
#
# Isso gerará uma chave hexadecimal de 64 caracteres (256 bits)
# Use uma chave diferente para JWT_SECRET e ENCRYPTION_KEY
